# TODO List

- [x] Fix modpack extraction nested folder detection logic.
- [x] Generate fallback `start.sh` and `start.bat` scripts if they are missing after extraction.
- [x] Fix server status detection on Windows (Java PID detection and process monitoring).
- [x] Implement Windows-compatible log tailing for reconnect on panel restart.
- [x] Align server registry reconnect logic to support Windows.

